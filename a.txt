docker stop front-end-container-no-nginx;docker rm front-end-container-no-nginx;docker build -t front-end-no-nginx .;docker run -d -p 5173:5173 --name front-end-container-no-nginx front-end-no-nginx



find . -type f -not -path '*/.git/*' -not -name '.env' -not -name 'all_files_content.txt' -not -name 'npc-logo.png' -print0 | while IFS= read -r -d '' file; do
    if ! git check-ignore --quiet "$file"; then
        echo "file: ${file}"
        cat "${file}"
        echo
        echo "---END-OF-FILE---"
        echo
    fi
done > all_files_content.txt


